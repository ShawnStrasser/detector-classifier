# data, weights, and results are not versioned
data/
archive/
.tmp/
*.parquet
*.duckdb
*.duckdb.wal
*.pth
*.pbix
*.csv
__pycache__/
.ipynb_checkpoints/
.venv/

# --- but the shippable beta package IS versioned ---
!models/
!models/**
!tests/
!tests/data/
!tests/data/**
!docs/img/
!docs/img/**

# --- the manual-review package for the user (GUIDs, channel numbers, labels, model outputs only) ---

# not redistributed / old notebook with saved outputs
docs/*.pdf
baseline/*.ipynb

# ODOT-specific staging download code: never committed
src/download/

# research candidates (only models/final_v1 ships) and ODOT staff review files (not published)
models/*candidate*/
review/
