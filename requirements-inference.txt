# Everything src/predict.py needs to turn raw hi-res events into detector predictions.
# CPU only; no torch, no sklearn, no scipy, no atspm.
duckdb>=1.0
pandas>=2.0
numpy>=1.24
pyarrow>=14.0
onnxruntime>=1.17
# onnxruntime is REQUIRED: the neural half of models/final_v2 (models/final_v2/gru.onnx)
# is evaluated by src/gru_onnx.py and there is no second runtime and no fallback.
# Optional: lightgbm>=4.0 (pulls in scipy). If it is not installed, predict.py scores the same model files
# with the built-in pure-numpy tree evaluator (src/lgbm_numpy.py) - identical output, same speed.
